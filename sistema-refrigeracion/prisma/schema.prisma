generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Cliente {
  id_cliente Int      @id @default(autoincrement())
  nombre     String
  apellido   String
  cuit       String   @unique
  telefono   String?
  email      String?
  createdAt  DateTime @default(now())
}